export class ApiError extends Error{constructor(message,code="REQUEST_FAILED",status=0){super(message);this.code=code;this.status=status;}}
export async function apiFetch(path,options={}){const response=await fetch(path,{credentials:"same-origin",...options,headers:{accept:"application/json",...(options.headers||{})}}),type=response.headers.get("content-type")||"";if(!type.includes("application/json")){if(type.includes("text/html"))throw new ApiError("La sesión administrativa venció. Abre nuevamente Configuración para iniciar sesión.","ACCESS_SESSION_EXPIRED",401);throw new ApiError("El servidor devolvió una respuesta inesperada.");}const payload=await response.json();if(!response.ok||!payload.ok)throw new ApiError(payload.error?.message||"No fue posible completar la operación.",payload.error?.code,response.status);return payload;}
